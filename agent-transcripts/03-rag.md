# RAG
Implemented paragraph-aware chunking, configurable overlap, Ollama embeddings, cosine retrieval, similarity thresholding, and source citation metadata. Added a deterministic embedding implementation for isolated tests.
